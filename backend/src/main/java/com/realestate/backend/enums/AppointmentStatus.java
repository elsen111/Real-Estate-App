package com.realestate.backend.enums;

public enum AppointmentStatus {
        PENDING,
        CONFIRMED,
        TENTATIVE,
        IN_PROGRESS,
        DELAYED,
        COMPLETED,
        CANCELLED_BY_AGENT,
        CANCELLED_BY_CLIENT,
        RESCHEDULED,
        NO_SHOW,
        REJECTED
}
