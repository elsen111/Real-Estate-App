package com.realestate.backend.enums;

public enum AppointmentStatus {
        PENDING,
        APPROVED,
        TENTATIVE,
        IN_PROGRESS,
        DELAYED,
        COMPLETED,
        CANCELLED,
        RESCHEDULED,
        NO_SHOW,
        REJECTED
}
