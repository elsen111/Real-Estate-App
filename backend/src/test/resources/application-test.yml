spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/real_estate_test_db
    username: postgres
    password: Elsen4444
    driver-class-name: org.postgresql.Driver

  liquibase:
    enabled: true
    change-log: classpath:db/changelog-master.yaml

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false

logging:
  level:
    org.springframework.security: WARN
    org.hibernate.SQL: WARN